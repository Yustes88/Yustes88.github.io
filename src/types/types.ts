export type MenuItem = {
  title: string;
  src: string;
}

export type BakeryMenuTypes = {
  image: string;
  title: string;
  id: string;
}

export type DeliveryDataTypes = {
  title: string;
  description: string;
    workingDays: string;
    workingHours: string;
    address: string;
}